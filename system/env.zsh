# Only set this if we haven't set $EDITOR up somewhere else previously.
if [[ "$EDITOR" == "" ]] ; then
  # Use sublime for my editor.
  export EDITOR='atom'
fi

export RUBYGEMS_GEMDEPS=-

export MAGNETO="/Users/jb/Code/traintracks/galactus/magneto"
export QUICKSILVER="/Users/jb/Code/traintracks/galactus/quicksilver"
export SAGE="/Users/jb/Code/traintracks/sage"
